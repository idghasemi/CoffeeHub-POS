# اصلاح باگ شارژ کیف پول

تاریخ اصلاح: ۱۴۰۵/۰۵/۰۸ (2026-07-30)

## مشکل

ورودی مبلغ شارژ دارای این محدودیت‌های HTML بود:

```html
min="1"
step="1000"
```

مرورگر مقدارهای معتبر را از عدد `1` با گام `1000` محاسبه می‌کرد؛ یعنی
`1`، `1001`، `2001` و غیره. به همین دلیل `100000` نامعتبر بود ولی
`100001` ثبت می‌شد.

## اصلاح

- گام ورودی مبلغ به `1` تغییر کرد تا همه مبالغ صحیح مثبت قابل ثبت باشند.
- اعتبارسنجی مبلغ در تابع مستقل `parseWalletChargeAmount` متمرکز شد.
- مبلغ صفر، منفی، اعشاری و غیرعددی قبل از ارسال به API رد می‌شود.
- تست رگرسیون برای مبلغ `100000` و جلوگیری از بازگشت `step="1000"` اضافه شد.

## اجرای تست

```bash
npm --prefix frontend test
```

نتیجه مورد انتظار: هر چهار تست باید پاس شوند.
